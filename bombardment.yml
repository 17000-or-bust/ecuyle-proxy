config:
  target: 'http://localhost:8000'
  processor: "./arsenal.js"
  http:
    pool: 12
  phases:
    - duration: 60
      arrivalRate: 1500
scenarios:
  - flow:
    - function: "loadData"
    - get:
        url: "/{{ randomInt }}"

